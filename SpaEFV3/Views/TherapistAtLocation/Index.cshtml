@model IEnumerable<SpaEFV3.Models.Xref_Location_Therapist>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Location.Loc_Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Therapist.Therapist_FName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Location.Loc_Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Therapist.Therapist_FName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Loc_Ther_Xref_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Loc_Ther_Xref_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Loc_Ther_Xref_ID })
        </td>
    </tr>
}

</table>
