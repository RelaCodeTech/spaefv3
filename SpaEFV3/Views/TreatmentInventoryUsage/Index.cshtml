@model IEnumerable<SpaEFV3.Models.Treatment_Inventory_Usage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Qty_Used)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.Product_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Treatment.Treatment_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Qty_Used)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Product_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Treatment.Treatment_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Treatment_Inventory_Usage_ID }) |
            @Html.ActionLink("Details", "Details", new { id = item.Treatment_Inventory_Usage_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Treatment_Inventory_Usage_ID })
        </td>
    </tr>
}

</table>
